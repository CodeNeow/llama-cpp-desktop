「本地聊天」页提供一个开箱即用的对话界面，直接与本地模型对话，数据不出本机。

## 开始对话

1. 先在「API 路由」页**启动服务**——聊天页依赖服务提供推理能力。服务未运行时，页面会显示提示卡，点「前往 API 页」即可跳转；
2. 回到聊天页，在顶部的模型下拉框中选择要对话的模型（列表来自服务已识别的模型）;
3. 在底部输入框输入消息，**Enter 发送，Shift+Enter 换行**；
4. 回复以流式方式逐字输出，结束后显示本条消息的生成速度（tok/s）。

支持多模态的模型（带 mmproj 文件）可以点输入框旁的附件按钮添加图片，让模型看图说话。

## 调整对话参数

点工具栏右侧的齿轮图标展开参数面板，可调整：

- **温度**：越高越发散，越低越稳定；
- **Top P / Top K**：控制候选词的采样范围；
- **重复惩罚**：减少重复啰嗦的输出；
- **最大生成 Token**：单次回复的长度上限；
- **系统提示词**：给模型的人设或任务说明，留空则不注入。

点「恢复默认」可一键还原所有参数。参数改动即时生效于下一次请求。

## 思考过程

推理类模型（如 DeepSeek-R1 系列）输出思考过程时，聊天页会把它折叠进「思考过程」块，点击可展开查看，正文只显示最终回答——两种内容分开统计速度。

## 对话记录

- 点「清空对话」删除当前会话；
- 对话记录保存在本机（浏览器本地存储），关闭应用后再次打开可以继续上一次对话。

聊天页发送的每一轮请求都走本地服务，不会上传任何数据到云端。
